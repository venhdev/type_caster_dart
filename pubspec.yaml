name: type_caster
description: A starting point for Dart libraries or applications.
version: 0.0.1+1.beta
repository: https://github.com/venhdev/type_caster_dart
home: https://github.com/venhdev/type_caster_dart

environment:
  sdk: ">=3.0.0 <4.0.0"

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.24.0
